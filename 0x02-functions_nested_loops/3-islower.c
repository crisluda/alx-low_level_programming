#include "main.h"

/**
 * _islower - this is the main function
 * @c: is an int
 * Return: the main have to return 0 or 1
 */

int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
	return (1);
	}
	return (0);

}
