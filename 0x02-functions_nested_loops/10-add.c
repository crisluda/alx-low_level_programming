#include "main.h"
/**
 * add - this is the main function
 * @a: is an int
 * @b: is an int
 * discription - the starting point of the program
 * Return: the main have to return 0
 */
int add(int a, int b)
{
int total;

	total = a + b;
		return (total);
}
